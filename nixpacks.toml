# Pin Python version for Railway/Nixpacks
# Prevents "default Python version changed" silent breakage
[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["python -m pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "python main.py"
